{% set name = "equilibrator-cache" %}
{% set version = "0.7.1" %}
{% set python_min = "3.11" %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/{{ name.replace('-', '_') }}-{{ version }}.tar.gz
  sha256: 6423e6b35841566a91b6733a560851a9f71151d6268f3fc96e777fc9a9466faa

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps

requirements:
  host:
    - pip
    - python {{ python_min }}
    - setuptools >=62.6
    - setuptools-scm
    - setuptools-git-versioning >=2.0,<3
  run:
    - python >={{ python_min }}
    - numpy >=2.0
    - scipy >=1.12
    - pandas >=2.3
    - pyarrow >=23.0
    - sqlalchemy ~=2.0
    - Levenshtein ~=0.27
    - pint ~=0.24
    - tqdm ~=4.0
    - appdirs ~=1.4
    - pooch ~=1.8

test:
  imports:
    - equilibrator_cache
    - equilibrator_cache.models
  requires:
    - pip
    - python {{ python_min }}
  commands:
    - pip check

about:
  home: https://gitlab.com/elad.noor/equilibrator-cache
  summary: Cache application for compounds, reactions, and enzymes
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - rgiessmann
    - Midnighter
    - eladnoor
